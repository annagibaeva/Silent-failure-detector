# Copy to .env and fill in values (never commit .env).
# Required only for ClaudeJudge published numbers: python -m eval.published

ANTHROPIC_API_KEY=

# Optional: default judge model (override per run with --model)
# JUDGE_MODEL=claude-opus-4-8
# Dev / draft runs: claude-sonnet-4-6
